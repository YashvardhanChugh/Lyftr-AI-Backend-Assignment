# Example environment variables for this project.
# Copy this file to `.env` and replace placeholder values with real secrets.
# Do NOT commit a real `.env` file; `.env` is already ignored by .gitignore.

# DATABASE_URL: SQLAlchemy/DB connection string used by the application.
# Example (SQLite file): sqlite:////data/app.db
# Example (Postgres): postgresql://user:pass@localhost:5432/dbname
DATABASE_URL=sqlite:////data/app.db

# WEBHOOK_SECRET: secret used to verify incoming webhooks (HMAC/shared secret).
# Placeholder only — do NOT use this value in production.
WEBHOOK_SECRET=change-me-placeholder

# LOG_LEVEL: logging verbosity. One of DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default used by the app is INFO.
LOG_LEVEL=INFO
